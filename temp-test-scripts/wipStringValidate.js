function inputValidation(string s) private pure returns (uint result, bool isTrue)
    {
        isTrue = false;
        bytes memory b = bytes(s);
        result = 0;
        uint i;
        for(i = 0; i < b.length; i++)
        {
            
        }
    }